# Screenshot Capture List

Add real screenshots here as part of the public polish sprint.

Recommended files:

- `auth-desktop.png`
- `auth-mobile.png`
- `dashboard-desktop.png`
- `dashboard-mobile.png`
- `categories-desktop.png`
- `transactions-desktop.png`

Recommended capture rules:

- use clean sample data
- avoid browser clutter where possible
- keep a consistent window size per device type
- update screenshots when the UI changes substantially
